/**
 * Development environment configuration
 *
 * To change the backend API URL:
 * 1. Update 'apiUrl' below to point to your backend server
 *
 * Examples:
 * - Relative URL (default, works with Quinoa): '/whisper/api'
 * - Full URL for standalone Angular: 'http://localhost:8080/whisper/api'
 * - Production URL: 'https://your-domain.com/whisper/api'
 */
export const environment = {
  production: false,
  apiUrl: '/whisper/api',
};
